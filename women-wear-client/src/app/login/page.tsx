import LoginUI from "@/components/UI/LoginUI/LoginUI";

export const metadata = {
  title: "Login",
};

const Login = () => {
  return (
    <div>
      <LoginUI></LoginUI>
    </div>
  );
};

export default Login;
